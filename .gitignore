node_modules/
*.tgz
.DS_Store

# Brainstorming companion: server state and the generated mockup pages.
.superpowers/

# mkdocs output. `mkdocs build` writes site/ by default; the workflow writes _site/.
site/
_site/

# hooks/nilam_assets.py is imported by mkdocs, so Python leaves bytecode beside it.
__pycache__/
*.py[cod]

# Visual-regression working files: the generated capture pages, the current run's PNGs and
# the diff images. All three are rebuilt by every `node test/visual.test.mjs`. The committed
# reference PNGs live in test/visual/baselines/ and are NOT ignored.
test/visual/out/
